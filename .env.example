# Slack Tokens
# User token (starts with xoxp-) - Required for reading unread messages and marking as read
SLACK_USER_TOKEN=xoxp-your-user-token-here

# Bot token (starts with xoxb-) - Required for sending DM summaries
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Your Slack user ID (starts with U) - Find it in your Slack profile → More → Copy member ID
SLACK_USER_ID=U01234ABCDE

# Optional: Slack signing secret for verifying interactions (future use)
# SLACK_SIGNING_SECRET=your-signing-secret-here

# OpenAI API Key
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: OpenAI model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Log level (default: INFO)
# LOG_LEVEL=DEBUG
